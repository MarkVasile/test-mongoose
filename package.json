{
  "scripts": {
    "dev": "NODE_ENV=development nodemon --watch 'src/**/*.ts' --exec 'ts-node -T -r ' tsconfig-paths/register src/index.ts | grep -v '/v1/log'"
  },
  "dependencies": {
    "mongoose": "^6.5.4",
    "typescript": "^4.8.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.0"
  }
}
